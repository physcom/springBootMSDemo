server:
  port: 5000

spring:
  cloud:
    config:
      name: config
      server:
        git:
          uri: https://github.com/physcom/springBootMSDemo
          search-paths: config-files*
          default-label: main
  application:
    name: config